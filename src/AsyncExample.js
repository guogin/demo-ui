import {getJSON, addHtmlToPage, addTextToPage, resetPageContent} from './Utils';

function asyncExample(e) {
    e.preventDefault();

    resetPageContent();

    //TODO: Implementation goes below

}

export default asyncExample;